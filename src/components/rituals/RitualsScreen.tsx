import { ProtocolBuilder } from "./ProtocolBuilder";

export const RitualsScreen = () => {
  return (
    <div className="space-y-6">
      <h2 className="text-2xl font-semibold">Rituals & Protocols</h2>
      <ProtocolBuilder />
    </div>
  );
};
